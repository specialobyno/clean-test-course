name: Lesson 5 DAST

on:
  push:
    branches:
      - lesson-5
jobs:
  security_test:
    needs: [deploy] # normally this would be the function_test job, but we're skipping that step here
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.14.0
        with:
          target: https://hangryhippo.quantic.host
      - uses: actions/checkout@v4
      - name: Install dependencies and run security scan
        run: npm install && npx snyk test
#  function_test: (commented out to save learner time)
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: frontend
#    steps:
#      - uses: actions/checkout@v4
#      - run: npm install
#      - name: Run functional tests
#        run: npm test -- --coverage --watchAll=false
  deploy:
#    needs: [function_test] (normally you'd not deploy until functional tests were complete)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
#      - run: npm install && npm run build (commented out to save learner time)
      - name: Generate deployment package
        run: zip -r deploy.zip build Dockerfile
      - name: '**PLACEHOLDER** Deploy to Elastic Beanstalk'
        run: echo Placeholder for Deployment
